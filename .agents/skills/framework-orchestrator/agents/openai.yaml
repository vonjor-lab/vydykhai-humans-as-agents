interface:
  display_name: "Framework Orchestrator"
  short_description: "Coordinate through hot or cold path"
  default_prompt: "Use $framework-orchestrator to coordinate this stream; keep ordinary task continuation on the lightweight hot path."
