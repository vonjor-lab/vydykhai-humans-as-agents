interface:
  display_name: "Accept Work"
  short_description: "Accept work in its execution context"
  default_prompt: "Use $accept-work in the owning execution context to verify the delivered contract and exact current artifact."
