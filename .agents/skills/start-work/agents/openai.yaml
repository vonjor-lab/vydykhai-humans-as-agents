interface:
  display_name: "Start Work"
  short_description: "Shape big topics into epics"
  default_prompt: "Use $start-work to turn this topic into an epic and task map."

policy:
  allow_implicit_invocation: true
